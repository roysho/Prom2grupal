using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DropVida : MonoBehaviour
{
    [SerializeField] private GameObject dropVida;

    // ESTA FUNCION IRIA PARA CUALQUER OTRO ENEMIGO QUE DROPEARÍA ITEM DE VIDA AL MORIR. Se llama esta funcion. 


    ////void DropMunicion()
    ////{
    ////    /if (life <= 0)
    ////    {
    ////        GameObject drop = Instantiate(dropVida);
    ////        drop.transform.position = transform.position;
    ////    }
    ////}


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
